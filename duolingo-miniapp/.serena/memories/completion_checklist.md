# Completion Checklist

- Ensure new/updated pages are registered in `app.json`.
- Verify WXML/WXSS/JS changes in WeChat Developer Tools (编译/预览).
- Confirm navigation paths and `wx.navigateTo` targets work.
- Check for console errors in DevTools.
- If using TDesign, ensure dependencies installed and page `usingComponents` updated.
